线程特定数据  from 《unix网络编程卷一》第26章 26.5


原理：
每个进程 维护一个 KEY数组 ： 该数组的每个元素有两个部分，一个部分标志该key是否被使用，一个部分存储一个对该key的特定数据的析构函数，当该线程结束时，自动调用该析构函数，析构掉线程的特定数据。

每个线程维护一个PKEY数组：该数组的每个元素，只包含一个部分，即KEY对应的相应数据。key-value


key由 KEY数组维护，标志该key ，是否被使用
value由线程自己维护，线程创建自己的key对应的数据，当线程使用该key时，便获取到自己的内容


