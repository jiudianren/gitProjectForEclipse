在fopen中FILE *pf = fopen(path,"r+");path为文件路径，“r+”为打开文件的方式。
其中“r+” 以可读写方式打开文件，该文件必须存在。
所以在程序的开头通过linux的open来打开文件（当文件不存在时候，则创造该文件）
在修改已经存在的某一具体行内容时候，通过fseek(pf,0,SEEK_SET）将文件指针stream的位置设置到开始的位置，在通过多文件读行，来定位到需要修改的行的位置。

文件修改结束后，fclose(pf);	来关闭文件。
